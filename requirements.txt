fastapi>=0.115,<1.0
uvicorn[standard]>=0.52.0,<1.0
PyYAML>=6.0,<7.0
bcrypt>=4.0,<6.0  # 5.0+ raises ValueError for passwords > 72 bytes (guard in auth.py)
# numpy 2.3 raised its minimum to Python 3.11 and 2.5 to Python 3.12. This
# project still targets Python 3.10 (see the test matrix in
# .github/workflows/python-app.yml), so cap below 2.3 to keep 3.10 resolvable.
# Raising this ceiling requires dropping Python 3.10 from the support matrix
# first -- otherwise the install simply fails to find a compatible wheel.
numpy>=1.26,<2.3
# install_python_deps.sh auto-selects onnxruntime-gpu when nvidia-smi can
# enumerate an NVIDIA device. Override with DAYGLE_ONNXRUNTIME_VARIANT=cpu|gpu.
# The GPU wheel's CUDA/cuDNN compatibility must match the installed driver;
# see README.md for Tesla P4 guidance.
#
# Ceiling held at <1.21 to stay on the CUDA 11/12-era ONNX Runtime line that
# still ships Pascal (compute capability 6.1, e.g. Tesla P4) sm_61 cubins.
# Newer ORT releases move to a CUDA stack that drops Pascal support, which
# silently breaks GPU inference on P4 deployments (session-create failure or a
# quiet fall back to CPU). This mirrors the onnxruntime-gpu>=1.18,<1.21 default
# in scripts/install_python_deps.sh -- bump both together, and only after
# confirming the target GPUs are still supported by the newer release.
onnxruntime>=1.18,<1.21
onnx>=1.22.0,<2.0
# torch.onnx._internal.exporter (used by Ultralytics YOLO.export)
# imports onnxscript at module load; not pulled in by ``onnx`` itself.
onnxscript>=0.7.1,<1.0
# Round-9 H1a: exact-pin the in-house known-good SDK release. The B3
# (R6) decision to rely on the Ultralytics SDK + pip TLS for ONNX
# delivery integrity assumes a single verified release. A minor-version
# range would silently absorb future 8.x.y PyPI drops -- bump this pin
# only after reviewing the upstream release notes for supply-chain
# concerns.
# Bumped to 8.4.107 on 2026-07-27 after reviewing upstream release notes.
ultralytics==8.4.113
# Bumped to the 5.x line on 2026-07-27. Audited every cv2 symbol this repo
# uses (imdecode/imencode, cvtColor, resize INTER_NEAREST/LINEAR, rectangle,
# putText/getTextSize, VideoWriter[_fourcc], VideoCapture + CAP_PROP_BUFFERSIZE,
# IMWRITE_JPEG_QUALITY, LINE_AA) against 5.0.0 -- all present and behavior-
# compatible. The 5.x text-rendering / nearest-neighbor changes only alter
# exact pixel values; no test here asserts pixel-exact output, so they are a
# no-op for us. Cap below 6.0 so a future major bump gets the same review.
opencv-python-headless>=5.0.0.93,<6.0
python-multipart>=0.0.32,<1.0
sounddevice>=0.5.5,<1.0
# CPU-only YAMNet sound classifier runtime.
ai-edge-litert>=2.1.6,<3.0
