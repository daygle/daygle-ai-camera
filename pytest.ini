[pytest]
testpaths = tests
# Coverage runs on every pytest invocation (locally and in CI). Drop these two
# lines if the extra seconds per run ever outweigh the visibility.
addopts = --cov=app --cov-report=term-missing
